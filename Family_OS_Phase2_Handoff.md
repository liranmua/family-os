# Family OS — מסמך מסירה לשלב 2 (סנכרון בין מכשירים דרך Firestore)

מסמך זה מיועד לסוכן קוד (Claude Code / Cursor / כל סוכן אחר) שיבנה את **שלב 2** בתוכנית התלת-שלבית של Family OS. נועד להיקרא **לצד** `CLAUDE.md` ו-`Family_OS_Phase1_Handoff.md` שבאותה תיקייה — לא במקומם. שלב 1 כבר חי ופרוס: https://liranmua.github.io/family-os/

## קרא קודם

1. `CLAUDE.md` — הקשר מלא, כולל הסעיף על שלב 1 החי.
2. `Family_OS_Phase1_Handoff.md` — מבנה הנתונים המקומי שנבנה בשלב 1 (Categories/People/Tasks/SubTasks/Routines/RoutineCompletions/Projects/Shopping_List).
3. המסמך החי (ERD + כל ההחלטות): https://claude.ai/code/artifact/25a6b84c-6c11-4dce-99c8-651a5b161370

## מה זה שלב 2, ומה זה **לא**

שלב 2 מוסיף **סנכרון בזמן אמת בין המכשירים של לירן ומורן**, דרך Firestore. זה הכל. לא נוגעים בשום דבר אחר.

**בתחום (Scope IN):**
- חיבור ל-Firestore (Cloud Firestore, מצב Native) בפרויקט Firebase הקיים `family-os-poc` — **לא** ליצור פרויקט Firebase חדש, הוא כבר קיים וחינמי מה-POC הקודם. רק להפעיל בו Firestore Database (אם עוד לא מופעל).
- כל הנתונים (Tasks/Routines/Projects/Shopping_List וכו') עוברים לחיות ב-Firestore, ומסתנכרנים בזמן אמת בין מכשיר של לירן ומכשיר של מורן.
- מיגרציה חד-פעמית אוטומטית של הנתונים המקומיים (ראו סעיף ייעודי למטה).

**מחוץ לתחום (Scope OUT) — במפורש, לא לגעת בזה בשלב הזה:**
- **בלי Google Sign-In / חשבונות משתמש אמיתיים.** זה שלב 3. בשלב 2 אין מסך התחברות בכלל.
- **בלי Google Calendar / Drive.**
- **בלי Push שמגיע מהשרת** כשהאפליקציה סגורה (FCM Admin SDK וכו') — עדיין מדובר רק בהתראות מקומיות כמו בשלב 1.
- בלי מיזוג חכם של קונפליקטים (merge UI) — ראו "ניהול קונפליקטים" למטה.

## מודל הגישה/אבטחה: נתיב פרטי קבוע בקוד

לירן בחר במפורש באפשרות הזו (במקום Firebase Anonymous Auth + התאמת מכשירים), אז ככה זה עובד:

- אין מסך התחברות. שני המכשירים (של לירן ושל מורן) מצביעים על אותו מסמך/נתיב קבוע ב-Firestore, שמוטבע בקוד של האפליקציה (למשל `families/{FAMILY_ID}/...` כאשר `FAMILY_ID` הוא מזהה קבוע וקשיח בקוד, לא נוצר דינמית ולא מוזן ע"י המשתמש).
- כללי אבטחה ב-Firestore (Security Rules) יגבילו קריאה/כתיבה רק לנתיב הזה הספציפי — לא "פתוח לכולם", אבל גם לא הגנה אמיתית ברמת משתמש, כי אין authentication.

**חשוב שלירן יידע (שקיפות על הטרייד-אוף, לא רק הערת קוד):** מכיוון שאין login אמיתי, מי שישיג את קוד ה-JS של האתר (שהוא ציבורי — כל אחד שנכנס לאתר יכול לראות אותו) יכול טכנית לגשת לאותו נתיב ב-Firestore. זה **בסדר גמור** לשימוש הזה — האפליקציה לא מכילה מספרי חשבון בנק, מידע רפואי, או כל דבר רגיש באמת, רק משימות/קניות/פרויקטים משפחתיים. זו בדיוק הסיבה שהאבטחה האמיתית (Google Sign-In, שבו רק לירן ומורן יוכלו להתחבר) נדחית לשלב 3 ולא מדולגת. הסוכן שבונה את שלב 2 צריך לציין את זה בבירור בדיווח ההתקדמות ללירן, לא רק להניח שהוא כבר יודע.

## מיגרציה מהנתונים המקומיים (שלב 1) ל-Firestore — לוגיקת בטיחות קריטית

לירן בחר "מעלים אוטומטית בפעם הראשונה". **חובה** לממש את זה עם הלוגיקה הבאה, לא סתם "לדחוף את המקומי":

1. כשהאפליקציה נטענת בפעם הראשונה עם קוד שלב 2, היא בודקת: **האם הנתיב הקבוע ב-Firestore כבר מכיל נתונים?**
2. **אם ריק** (אף מכשיר עוד לא סנכרן) → מעלים את הנתונים המקומיים של המכשיר הזה (מ-IndexedDB) ל-Firestore. מהרגע הזה, Firestore הוא מקור האמת.
3. **אם לא ריק** (מכשיר אחר כבר סנכרן קודם) → **לא** דורסים! מושכים את הנתונים הקיימים מ-Firestore ומציגים אותם, ומתעלמים מהעותק המקומי הישן של המכשיר הזה (או שומרים אותו בצד כגיבוי, לא מוחקים, אבל לא מציגים כברירת מחדל).

**זו הסיבה שהסדר קריטי: לירן חייב לפתוח את האפליקציה שלו (עם הנתונים האמיתיים ששלב 1 כבר צבר) לפני שמורן פותחת את הגרסה המעודכנת אצלה.** אם מורן תפתח קודם עם מכשיר "ריק", ה-Firestore הריק שלה יהפוך למקור האמת, והנתונים האמיתיים של לירן לא יעלו אליו אוטומטית (כי לפי החוק בסעיף 3, Firestore כבר "לא ריק"). זו נקודה שהסוכן חייב לכתוב בבירור בדיווח לפני שהוא סוגר את הפיצ'ר, ושלירן קיבל את זה ישירות בצ'אט (לא רק בקובץ).

## ניהול קונפליקטים (עדכונים בו-זמנית משני מכשירים)

**גישה פשוטה בכוונה, תואמת לעיקרון "מתחילים לאט" של הפרויקט: Last-write-wins.** אם לירן ומורן עורכים את אותו פריט ממש באותו רגע (נדיר, לזוג משתמשים), הכתיבה האחרונה שמגיעה ל-Firestore היא זו שנשארת. אין ממשק מיזוג, אין התראה על קונפליקט. אם בעתיד זה יתברר כבעיה בפועל (לא צפוי לזוג עם שימוש רגיל) — אפשר לשקול שיפור בשלב מאוחר יותר.

## שכבת האחסון: לוותר על IndexedDB, לעבור ל-Firestore SDK עם offline persistence מובנה

ב-Firestore JS SDK יש מנגנון offline persistence מובנה (`enableIndexedDbPersistence` / ברירת המחדל ב-SDK החדש) שכבר עושה בדיוק את מה שה-IndexedDB הידני של שלב 1 עשה — שמירה מקומית + עבודה אופליין — אבל גם מסנכרן אוטומטית כשיש רשת. **מומלץ להחליף לגמרי את שכבת ה-IndexedDB הידנית שנבנתה בשלב 1 בשכבת ה-persistence המובנית של Firestore**, כדי שלא יהיו שתי מערכות אחסון שיכולות להתבדר זו מזו. זה אומר שינוי בקוד הגישה לנתונים (data layer), לא רק "הוספת סנכרון מעל" — הסוכן צריך לתכנן את זה כרפקטור מסודר, שלב-אחר-שלב לפי הישויות (בדיוק כמו העיקרון משלב 1: ישות אחת בכל פעם, מאומתת מקצה לקצה, לא הכל בבת אחת).

## הערת מכסה (Quota) חינמית

Firestore בתוכנית החינמית (Spark) נותן מכסה יומית נדיבה אבל לא אינסופית. **חובה להשתמש ב-`onSnapshot` (listeners בזמן אמת) ולא ב-polling** (קריאה חוזרת כל X שניות) — כך שני מכשירים לזוג משפחתי נשארים רחוק מאוד מהמכסה החינמית. זה גם פשוט יותר לממש וגם נותן את חוויית "זמן אמת" שהמטרה של שלב 2 היא בעצם.

## עקרונות עבודה — תזכורת משלב 1 (עדיין תקפים)

1. בלי קפיצה בין כלים/גישות באמצע — סטאק אחד, מחויבות מלאה.
2. שקיפות מלאה בין "עובד באמת מול Firestore" ל"עדיין רק מקומי" בכל דיווח התקדמות.
3. צעדים קטנים ומאומתים: קודם לוודא שהחיבור הבסיסי ל-Firestore עובד (כתיבה+קריאה של רשומה אחת, נראה בשני מכשירים), ורק אז להעביר ישות-ישות.
4. אם מתעורר ספק לגבי משהו שלא מכוסה כאן — לעצור ולשאול את לירן, לא להחליט לבד ולהמשיך.

## קריטריון סיום לשלב 2

לירן ומורן פותחים כל אחד את האפליקציה במכשיר שלו (אותו URL: https://liranmua.github.io/family-os/), ורואים **את אותם נתונים בדיוק**. שינוי שנעשה במכשיר אחד (הוספת משימה, סימון כבוצע) מופיע אצל השני תוך שניות, בלי לרענן ידנית. הנתונים ששלב 1 כבר צבר לא אבדו. האפליקציה עדיין עובדת (עם קאש אופליין) גם כשאין אינטרנט זמנית. רק אחרי שזה עובד בנוח — עוברים לשלב 3 (Google Sign-In / Calendar / Drive).
