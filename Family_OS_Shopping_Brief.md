# Family OS — תדריך תכנון לאיזור הקניות (לפני הבנייה)

מסמך זה ממשיך את הסעיף על "רשימת קניות" ב-`Family_OS_UX_Redesign_Brief.md` (שם זה תואר בקצרה כ"קטגוריה עצמאית") — לירן ביקש לעצור ולתכנן את האיזור הזה לעומק **לפני** שממשיכים לבנייה, כי יש בו כמה החלטות מוצר של ממש, לא רק מסך UI. נועד להיקרא לצד `CLAUDE.md` וכל שאר מסמכי המסירה בתיקייה.

## הרעיון המקורי של לירן

איזור קניות שמפריד בין כמה סוגי רשימות (סופר, סופר-פארם, מוצרים/רהיטים נוספים לבית), עם שאיפה עתידית לתמחור אוטומטי וזיכרון של פריטים חוזרים עד כדי "סל צריכה משפחתי" שמקצר את בניית הרשימה השבועית, כולל שדות כמות והערות לכל פריט.

## החלטה 1: תמחור אוטומטי — נדחה, לא בסבב הזה

לירן הפנה לסקיל `israeli-product-price-comparator` כבסיס אפשרי לתמחור. **נבדק בפועל** לפני שהוחלט משהו: הסקיל הזה **מפורש שהוא לא מכסה מזון/מצרכי סופר** (רק אלקטרוניקה, מכשירי חשמל, בית-וגינה, פארם/קוסמטיקה) — ומפנה במפורש למקום אחר לשאלות תמחור סופר. בדקנו גם אם יש שרת/חיבור MCP ייעודי למחירי סופרמרקט בישראל — **לא נמצא כזה זמין כרגע**.

תמחור אוטומטי אמיתי למוצרי סופר **אפשרי טכנית עקרונית** — רשתות שיווק מחויבות בחוק (תקנות שקיפות מחירים) לפרסם קבצי מחירים פומביים — אבל בפועל זה נתון:
- מפוצל לפי רשת ולפי סניף (אין מקור אחד מאוחד).
- מתפרסם כקבצים גולמיים (XML/GZIP), לא כ-API נוח.
- דורש התאמה בין השם שהמשתמש מקליד ("חלב 3%") לבין השם הרשמי בקטלוג הרשת — משימת fuzzy-matching אמיתית על אלפי פריטים.

זה פרויקט טכני נפרד ומשמעותי, לא תוספת קטנה. **לירן אישר לדחות אותו** — לא מבוטל, רק לא חלק מהסבב הזה. אם ירצה לחזור לזה בעתיד, זו תהיה חקירה/סבב נפרד לגמרי (כולל בדיקת היתכנות רצינית לפני שמתחייבים לבנייה), לא הרחבה של הסבב הנוכחי.

**מה כן נכנס לתמונה במקום תמחור אוטומטי**: זיכרון פריטים חוזרים (ראו סבב 2 למטה) — נותן חלק ניכר מהתועלת המקורית (לקצר את בניית הרשימה) בלי שום תלות בנתוני מחיר חיצוניים.

## החלטה 2: מבנה — שלוש רשימות נפרדות לפי סוג חנות

לא רשימה אחת עם תיוג/סינון — **שלוש רשימות נפרדות**, כל אחת עם תצוגה משלה:
1. **סופר** (סופרמרקט) — הרשימה המרכזית, זו שתקבל בהמשך את פיצ'רי הזיכרון/סל (סבב 2).
2. **סופר-פארם**.
3. **בית / רהיטים / מוצרים נוספים** — לירן ציין שזה עדיין "בגדר בדיקה" מבחינתו (לא בטוח עדיין שזה צורך חוזר קבוע) — לבנות אותה כרשימה פשוטה כמו סופר-פארם, בלי להשקיע בה מעבר לזה בשלב הזה.

**המלצה למימוש ה-UI** (בהתאם לתבנית שכבר קיימת באפליקציה — מסך עם מבנה פנימי, לא ריבוי אריחים בעמוד הראשי): האריח "קניות" במסך הראשי נשאר אריח אחד, אבל בכניסה אליו יש בחירה/טאבים בין שלוש הרשימות — בדיוק כמו שמסך "משימות" מכיל דשבורד פנימי ולא הפך לכמה אריחים. **לתשומת לב הסוכן**: אם ה"רצועת היום" במסך הראשי מציגה כרגע "פריטים חסרים מרשימת קניות" (כפי שתוכנן ב-UX Brief) — להחליט אם הספירה הזו מסכמת את כל שלוש הרשימות או רק את הסופר; ברירת המחדל המוצעת היא סיכום כל השלוש, אלא אם לירן יגיד אחרת כשהוא רואה את זה בפועל.

## סבב 1 (עכשיו): פיצול + שדות כמות/הערות

זה מה שנבנה בפועל בסבב הזה:
- שינוי סכימה: `Shopping_List` מקבל שדה `storeType` (סופר / סופר-פארם / בית-אחר) במקום רשימה שטוחה אחת.
- שני שדות חדשים לכל פריט: **כמות** (מספר/טקסט חופשי, כמו "2 יחידות" או "1 ק״ג") ו-**הערות** (טקסט חופשי, כמו "הסוג האורגני").
- כל רשימה עובדת עצמאית: הוספה/סימון-כנקנה/מחיקה בתוך הרשימה שלה.
- עיצוב עקבי לשלוש הרשימות (לא שלושה סגנונות שונים) — ה"סופר" היא זו שתקבל תוספת בסבב 2, לא שינוי מבני נוסף.

## סבב 2 (בהמשך, אחרי שסבב 1 עובד ומאומת): זיכרון פריטים חוזרים → "סל משפחתי"

**לא נבנה עכשיו** — מתועד כאן כדי שהכיוון יהיה ברור מראש, לפי אותו עיקרון שכבר עבד היטב בפרויקט הזה (שלב אחד בכל פעם, לא לקפוץ קדימה):

- האפליקציה זוכרת פריטים שכבר נוספו בעבר לרשימת הסופר (שם + כמות/הערות אחרונות שנרשמו).
- כשמתחילים רשימה חדשה, אפשר להוסיף פריטים חוזרים במהירות (הצעה/auto-complete מתוך ההיסטוריה, לא הקלדה מאפס בכל פעם).
- שאיפה בהמשך: "סל צריכה משפחתי" — קבוצת פריטים קבועה שקונים כמעט תמיד, שאפשר להוסיף בלחיצה אחת לרשימה חדשה ולערוך משם.
- ממוקד ברשימת **הסופר** בלבד (שם יש הכי הרבה חזרתיות) — לא בהכרח נדרש בסופר-פארם/בית-אחר, אלא אם יתברר שיש ערך גם שם.

## פריט עתידי נוסף (לא בסבב הזה, לא בסבב 2 — רק תיעוד לזיכרון)

לירן ציין רעיון עתידי: לקשר מוצרים ספציפיים שצריך לקנות **לפרויקט** מסוים, כך שהמוצר יופיע גם בפרויקט (כמו שמשימות מקושרות מוצגות היום בתוך מסך הפרויקט) וגם ברשימת הקניות הרלוונטית — אותה תבנית של "נתון אחד, שתי תצוגות" שכבר קיימת בין משימות לפרויקטים. זה רעיון ל**עתיד לא מוגדר**, לא חלק מהתכנון הנוכחי של איזור הקניות — לא לבנות תשתית מיוחדת בשבילו עכשיו, רק לא לסתור אותו במבנה הנתונים (למשל, לא להניח שפריט קניות "שייך" תמיד לרשימה אחת בלבד בלי אפשרות הרחבה עתידית).

## מה לא משתנה

שאר האיזורים (משימות, פרויקטים, שגרות, יומן, פיננסים) לא נוגעים לסבב הזה. סדר הבנייה המוסכם אחרי שהקניות (סבב 1) מוכן: פיננסים → יומן → המשך עיצוב משימות.
