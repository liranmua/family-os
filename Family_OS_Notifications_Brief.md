# Family OS — מנגנון התראות (סבב 1: פנימיות, סבב 2: Push אמיתי)

לירן ביקש להתקדם למנגנון ההתראות. לפני מסירה לסוכן הקוד נבדקו בפועל (חיפוש רשת, לא ניחוש) שלוש שאלות טכניות פתוחות: האם אפשר Push אמיתי בלי לעבור לתוכנית בתשלום (Blaze) של Firebase, ואיך; והאם יש דרך פשוטה וחינמית לשלוח התראות ל-WhatsApp. הממצאים קבעו את החלוקה לשני סבבים למטה.

## החלטת לירן: שני מסלולים במקביל, לא אחד במקום השני

לירן בחר להתחיל עכשיו עם **התראות פנימיות** (סבב 1, הפשוט), אבל ביקש **גם** לקדם **Push אמיתי** (סבב 2) לרמה הכי גבוהה שאפשר בלי לשלם — לא לדחות אותו ללא הגבעה כמו שנדחה בעבר. שאל גם על אפשרות ל-WhatsApp — נבדקה ישירות (ראו למטה) ואינה מומלצת בשלב הזה.

## סבב 1 (לבנות עכשיו): התראות פנימיות

התראות שמופיעות **רק כשהאפליקציה פתוחה בפועל** בדפדפן/PWA (Web Notifications API בתוך הדף) — בלי שום שרת, מפתח, או הגדרה חיצונית. זה בדיוק אותו מנגנון שתוכנן כבר בשלב 1 של הפרויקט (לפני חודשים, אז נדחה יחד עם ה-Push) — עכשיו הזמן לבנות אותו בפועל.

**על מה מתריעים** (לפי בחירת לירן, כל השלושה):
1. **בלוקים מהלוח השבועי המשפחתי** (`weeklyBlocks`) — התראה כש"מתקרב" בלוק (ברירת מחדל מוצעת: 15 דקות לפני שעת ההתחלה — ניתן לשנות אחרי שרואים את זה בפועל).
2. **משימות עם תאריך/שעת יעד מתקרבים** — התראה כש"מתקרב" (ברירת מחדל מוצעת: שעה לפני אם יש שעת יעד מדויקת; אם יש רק תאריך בלי שעה — התראה בבוקר של יום היעד).
3. **פריטים ברשימת קניות** — כאן הדבר שמתריעים עליו פחות מובן מאליו מהשניים הקודמים (אין "זמן יעד"). **ברירת מחדל מוצעת**: התראה כשהצד השני מוסיף פריט/ים לרשימה משותפת (כדי לדעת לבדוק לפני שיוצאים לקניות) — לא תזכורת מבוססת-זמן. אם לירן התכוון למשהו אחר (למשל תזכורת יומית קבועה "יש קניות ברשימה"), אפשר לשנות ברגע שרואים את זה בפועל.

**איך זה מוצג**: התראה בתוך הדף (toast/באנר), לא באנר מערכת ההפעלה — כי בלי הרשאת Push אמיתית (סבב 2) הדפדפן לא יכול להציג התראת מערכת כשהאפליקציה סגורה.

## סבב 2 (הבא בתור מיד אחרי סבב 1): Push אמיתי — בלי לעבור לתוכנית בתשלום

**נבדק בפועל**: Firebase Cloud Messaging (FCM) עצמו **חינמי לחלוטין וללא הגבלה**, בכל תוכנית כולל Spark (החינמית שהאפליקציה כבר משתמשת בה). הבעיה היחידה היא **מי מפעיל את הבדיקה התקופתית** ("האם יש משהו שמתקרב עכשיו, שצריך לשלוח עליו Push?") — Cloud Functions המתוזמנות (Scheduled Functions) של Firebase עצמו **דורשות** תוכנית Blaze (תשלום-לפי-שימוש, עם כרטיס אשראי רשום — גם אם בפועל לא ייגבה כלום כי השימוש נשאר בתוך המכסה החינמית).

**הפתרון שנמצא**: לעקוף את Cloud Functions לגמרי, ולהשתמש ב-**GitHub Actions המתוזמן** — בדיוק אותו מנגנון שכבר מפרסם את האתר בכל push (חינמי, כבר פעיל בפרויקט). מוסיפים לו תזמון (למשל כל 15 דקות) שמריץ סקריפט קטן (Node.js), עם `firebase-admin`, שבודק ב-Firestore מה "מתקרב" (אותה לוגיקה משלושת הסוגים למעלה) ושולח Push דרך FCM לכל מכשיר רשום. **אין תוכנית Blaze, אין כרטיס אשראי, אין עלות** — רק שימוש חוזר בתשתית שכבר קיימת.

**מה נדרש כדי שזה יעבוד** (טכני, לסוכן):
1. שמירת "מזהה מכשיר" (FCM device token) עבור כל משתמש מחובר ב-Firestore (הבסיס לזה כבר הוכח ב-POC מוקדם של הפרויקט — התקבל טוקן אמיתי מהטלפון של לירן; צריך לחבר את זה לזרימת ה-Sign-In האמיתית ולשמור טוקן לכל אחד מהם).
2. יצירת מפתח Service Account אחד ב-Firebase Console, ושמירתו כ-**GitHub Secret מוצפן** (חד-פעמי, לא ידני בכל פעם) — זה שונה מהותית מהחיכוך הקודם (שם ביקשנו ממנו להריץ סקריפט Push ידני עם המפתח על המחשב שלו בכל פעם); כאן זו הגדרה חד-פעמית מודרכת, בדיוק כמו אישורי ה-OAuth החד-פעמיים שכבר עשה בעבר (Google Sign-In, Calendar).
3. סקריפט תזמון ב-GitHub Actions (`.github/workflows`) שרץ כל X דקות ומפעיל את הבדיקה+שליחה.

**חשוב להבהיר ללירן מראש**: זה סבב עם עבודה טכנית אמיתית (לא שינוי קטן) — סקריפט חדש, אינטגרציה עם FCM, וטיפול נכון במפתח הרגיש (לא נחשף בקוד, רק ב-GitHub Secret). לא לבנות באותו push כמו סבב 1.

## WhatsApp — נבדק, לא מומלץ בשלב הזה

לירן שאל אם אפשר גם התראת WhatsApp. **נבדק ישירות**: שליחת הודעות WhatsApp אוטומטיות (לא ידניות מהאפליקציה הרגילה) דורשת **WhatsApp Business Cloud API** של Meta — הקמת חשבון עסקי מאומת, מספר טלפון עסקי ייעודי, אישור תבניות הודעה מראש מול Meta, ועלות לפי הודעה מעבר למכסה חינמית מוגבלת. זו הקמה ברמת "אינטגרציה עסקית", לא תוספת קטנה לאפליקציה משפחתית — משמעותית יותר מורכבת מ-Push אמיתי (סבב 2), בלי תועלת ברורה מעבר לזה (Push כבר מגיע ישירות לטלפון). **המלצה**: לא לבנות את זה עכשיו. Push אמיתי (סבב 2) נותן את אותה תועלת בפועל — התראה שמגיעה לטלפון גם כשהאפליקציה סגורה — בלי ההקמה העסקית. אם לירן עדיין ירצה WhatsApp אחרי שיתנסה ב-Push, זו תהיה בדיקת היתכנות נפרדת לגמרי בעתיד, לא המשך ישיר של הסבב הזה.

## מה לבנות עכשיו (רק סבב 1)

1. מנגנון התראות פנימיות בתוך האפליקציה (toast/באנר), פעיל רק כשהאפליקציה פתוחה.
2. שלוש לוגיקות בדיקה: בלוק שבועי מתקרב (15 דק' לפני, ברירת מחדל), משימה עם יעד מתקרב (שעה לפני / בוקר יום היעד), הוספת פריט קניות ע"י הצד השני.
3. **לא** לגעת עדיין ב-FCM/Service Account/GitHub Actions — זה סבב 2, מיד אחרי שסבב 1 עובד ומאומת.

## מה לא בסבב הזה בכלל

WhatsApp (נשלל לפי המחקר למעלה, לא רק נדחה). היסטוריית התראות/סימון-כנקרא. התאמת זמני-התראה אישיים לכל סוג (ברירות המחדל למעלה קבועות בסבב 1; אפשר להפוך לניתן-להגדרה בעתיד אם יתברר שצריך).
