# Family OS — תדריך עיצוב ומבנה (UX/IA) — סבב מיקוד ברכיבים

מסמך זה שונה באופיו משלושת מסמכי המסירה הקודמים (Phase1/2/3) — הוא לא תשתית, אלא **עיצוב ומבנה** (Information Architecture) של האפליקציה, אחרי ששלבי התשתית (מקומי → סנכרון → Sign-In) עובדים. נועד להיקרא לצד `CLAUDE.md` ושלושת מסמכי המסירה הקודמים באותה תיקייה. **המצב הנוכחי של האפליקציה** (https://liranmua.github.io/family-os/): Sign-In עם Google עובד ומאומת אצל לירן; Security Rules **לא** ננעלו עדיין ליוזמתו (מושהה עד אחרי הסבב הזה); סנכרון Firestore בין מכשירים עובד.

## למה זה קורה עכשיו

לירן ביקש במפורש להשהות את הידוק האבטחה (רשימה סגורה) ואת חיבור מורן, ולחזור למיקוד בעיצוב ובבניית הרכיבים בתוך האפליקציה — **כולל חיבור היומן**, שממשיך במקביל (זה לא מושהה, רק הנעילה/מורן מושהים).

## סדר העבודה (כפי שלירן ביקש במפורש)

1. **קודם: לגבש מחדש את התכנון/המבנה הכללי** (המסמך הזה) — לא לקפוץ ישר לפרטים.
2. **אחר כך: לבנות/לדייק כל אזור בנפרד**, אחד בכל פעם — אותו עיקרון שכבר עבד היטב בשלבים 1-2 (לא לקפוץ בין הכל בבת אחת).
3. **בסוף: לחזור למסך המשימות** לדיוקים/תיקונים נוספים שלא קשורים למבנה הכללי — אלה יגיעו כרשימה נפרדת אחרי שהמבנה החדש עומד על רגליו.

## המבנה החדש של המסך הראשי

המסך הראשי הופך למרכז (hub) עם ארבעה אזורים נפרדים, במקום "הכל בתוך מסך משימות אחד":

1. **משימות** — לא עוד המסך שנכנסים אליו ישירות; הופך ל**אזור נפרד** שנכנסים אליו בלחיצת כפתור מהמסך הראשי.
2. **רשימת קניות** — יוצאת מתוך "משימות" והופכת ל**קטגוריה עצמאית משלה**, לצד משימות במסך הראשי (לא עוד תת-חלק בתוך משימות).
3. **יומן** — אזור חדש שמציג אירועים מהיומן המשותף של גוגל, **מתעדכן כל יום**. זהו בדיוק חיבור היומן שתוכנן בשלב 3 (קריאה בלבד — לא כותב/עורך אירועים) — רק שמיקומו במסך הראשי, לא בתוך משימות. **הערה טכנית לסוכן**: ה-Sign-In הקיים כנראה ביקש רק scope בסיסי (זיהוי/מייל); הוספת `calendar.readonly` עשויה לדרוש אישור scope נוסף (הרשאה הדרגתית / incremental authorization) — לבדוק ולטפל בזה, לא להניח שזה כבר כלול.
4. **פיננסים** — יוצא מתוך מסך המשימות (איפה שהוא נמצא היום) והופך ל**אזור עצמאי** משלו במסך הראשי, לא עוד כרטיס בתוך משימות.

## פרויקטים — עיצוב חדש + קישורים

- מסך הפרויקטים מקבל **עיצוב חדש** (באותו סבב עם מסך המשימות).
- **יכולת חדשה**: הצמדת **קישורים** (לא קבצים/תמונות בפועל בשלב הזה — לירן בחר במפורש להתחיל רק עם קישורים) למשימות בתוך פרויקט, כדי לאגד הכל תחת "תיקייה" אחת מבחינה קונספטואלית — למשל קישור לתמונה/מסמך ששמור כבר ב-Drive או במקום אחר.
- **עתיד, לא בסבב הזה**: לירן ציין שאיפה ל**אחסון קבצים אמיתי** בתיקיות מסודרות בדרייב שלו. זה נדחה במפורש להמשך — יתכן שיתמזג עם היכולת של דרייב ששלב 3 כבר תכנן ברמת ה-scope (עיון/חיפוש בתיקייה משותפת). לא לבנות את זה עכשיו, רק לשים לב שהמבנה של שדה ה"קישור" לא יסתור פיתוח עתידי כזה.

## מסך משימות

- מקבל **עיצוב חדש**, באותו סבב עם פרויקטים.
- כרטיס הפיננסים **יוצא ממנו** (עובר לאזור העצמאי במסך הראשי — ראו למעלה).
- רשימת הקניות **יוצאת ממנו** (הופכת לקטגוריה עצמאית במסך הראשי — ראו למעלה).
- תיקונים/דיוקים נוספים במסך הזה **יגיעו בסבב נפרד**, אחרי שהמבנה הכללי החדש עומד — לא חלק מהסבב הזה.

## מה לא השתנה

- הנתונים עצמם (Tasks/Routines/Projects/Shopping_List/SubTasks וכו') — זה סבב עיצוב ומבנה, לא שינוי סכימה. אם שינוי מבנה נתונים כן נדרש (למשל שדה קישורים חדש בפרויקטים) זה תוספת ממוקדת, לא רה-פקטור.
- מודל האבטחה (Sign-In עובד, Security Rules לא ננעלו) — נשאר כפי שהוא, ביוזמת לירן, עד שיחליט לחזור לזה.
- חיבור מורן — מושהה, לא חלק מהסבב הזה.
