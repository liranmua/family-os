# Family OS — תיקונים במשימות ובפרויקטים (לפני קניות)

מסמך זה נועד לסוכן הקוד, כהמשך ישיר ל-`Family_OS_UX_Redesign_Brief.md`. לירן ביקש לעצור לפני שממשיכים לשלב הבא (קניות) ולתקן כמה דברים במשימות ובפרויקטים תחילה. **סדר הבנייה המוסכם נשאר כפי שסוכם — רק נוסף עצירה ממוקדת כאן לפני קניות.**

## פרויקטים

**1. תצוגת פרויקט מלאה (drill-in), לא רק כתיבת עדכון מהכרטיס**

כרגע אפשר לכתוב עדכון ישירות מכרטיס הפרויקט במסך הראשי, אבל אין דרך "להיכנס" לפרויקט ולראות תמונה מלאה. נדרש: לחיצה על כרטיס פרויקט פותחת **מסך פרויקט מלא**, הכולל:
- **לוג עדכונים** — היסטוריית העדכונים שכבר נשמרים (`updatesLog`, קיים משלב 1) מוצגת כרשימה בתוך מסך הפרויקט, לא רק אפשרות להוסיף עדכון חדש.
- **קישורים** — רשימת הקישורים שהוצמדו לפרויקט + כפתור הוספת קישור (כותרת+כתובת). זו התכונה שתוכננה ב-`Family_OS_UX_Redesign_Brief.md` ועדיין לא נבנתה בפועל.
- **משימות הפרויקט כ"תתי-משימות"** — ראו סעיף 2.

**2. משימות פרויקט מוצגות בשני מקומות, עם התקדמות**

הקישור בין משימה לפרויקט כבר קיים בסכימה (שדה `ProjectID` על Tasks, מוגדר עוד ב-Phase1). מה שחסר הוא **תצוגה**: משימות ששייכות לפרויקט צריכות להופיע:
- כרגיל, במסך המשימות (כמו כל משימה אחרת).
- **וגם** בתוך מסך הפרויקט עצמו, בתצוגה דומה לתתי-המשימות שכבר קיימת במשימות תהליכיות (checklist עם סימון V) — כך שסימון התקדמות על המשימות בתוך מסך הפרויקט מעדכן את אחוז ההשלמה של הפרויקט (הלוגיקה הזו כבר קיימת חלקית — התקדמות מחושבת מהמשימות המקושרות, לא נקבעת ידנית — רק חסרה התצוגה המשולבת בתוך מסך הפרויקט).
- זו תוספת ממשק (UI), לא שינוי סכימה — אותו נתון, שתי תצוגות.

## משימות — עיצוב דשבורד מחדש

מסך המשימות מקבל דשבורד ברור יותר:
- **תצוגה ברורה של משימות דחופות, מקובצת לפי קטגוריה.**
- **מונה משימות פתוחות** — כמה משימות פתוחות יש, מוצג בבירור בראש המסך.
- **חיפוש משימות** — לפי שם משימה, כולל התאמה חלקית (לא רק התאמה מדויקת).

## שינוי בשדה "אחראי" — חשוב: זה שינוי מכיוון עקרון ברזל, לא סתם relabel

לירן ביקש: בכל מקום שכרגע רשום "אחראי יחיד" עם בחירה בין לירן/מורן, שיהיה כתוב **"מוביל"**, ושהבחירה תהיה **מרובה** (אפשר לבחור גם את שניהם יחד).

**הוצג לו במפורש**: זה נוגד את "עקרון הברזל" שנקבע כבר בתחילת הפרויקט (מבוסס על ניתוח ההיסטוריה הקודמת בג'ימיני) — שלכל משימה יש אחראי יחיד, אף פעם לא "שנינו", כי זה זוהה כגורם ישיר לכך שמערכות ניהול-בית משפחתיות דומות ננטשות (משימה "משותפת" הופכת בפועל למשימה של אף אחד). לירן אישר שהוא רוצה לשמר את העיקרון הזה, ובחר בפתרון הבא:

**הפתרון שנבחר**: שני שדות נפרדים, לא אחד.
- **"מוביל"** — נשאר שדה **יחיד וחובה**, בדיוק כמו "אחראי" היום. זה רק שינוי שם התווית (מ"אחראי" ל"מוביל"), לא שינוי בכלל האכיפה — עדיין אי אפשר לשמור משימה בלי לבחור מוביל יחיד, ועדיין אין אופציית "שניהם" בשדה הזה.
- **"גם רלוונטי ל-"** (שדה חדש, אופציונלי) — בחירה מרובה (אפשר את שניהם, אחד, או אף אחד), לתיוג משימות שבאמת נוגעות ליותר מאדם אחד, בלי לדלל את האחריות הברורה של ה"מוביל". למשל: לירן מוביל, אבל "גם רלוונטי ל" מורן — כי היא צריכה לדעת/מעורבת.

**לסוכן**: להוסיף שדה חדש (`alsoRelevantTo` או דומה, array) לצד השדה הקיים (ששמו בתצוגה משתנה ל"מוביל", אבל נשאר יחיד וחובה בסכימה/באכיפת הטופס). לא לגעת בכלל האכיפה הקיים של השדה הקיים.

## הערה כללית

אחרי שהתיקונים האלה (פרויקטים + דשבורד משימות + שדה מוביל/רלוונטי) מוכנים ומאומתים — חוזרים לסדר המוסכם: קניות → פיננסים → יומן → (פרויקטים+קישורים כבר נכלל כאן) → המשך עיצוב משימות.
