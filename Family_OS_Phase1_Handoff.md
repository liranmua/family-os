# Family OS — מסמך מסירה לשלב 1 (Prototype PWA מקומי)

מסמך זה מיועד לסוכן קוד (Claude Code / Cursor / כל סוכן אחר) שיבנה את **שלב 1** בתוכנית התלת-שלבית שסוכמה עבור פרויקט Family OS של לירן ומורן. הוא נועד להיקרא **לצד** `CLAUDE.md` שבאותה תיקייה (שם ההקשר המלא, ה-POC-ים, וההיסטוריה) — לא במקומו.

## קרא קודם

1. `CLAUDE.md` (בתיקייה הזו) — הקשר מלא: מה כבר קיים, החלטות ארכיטקטורה, ה-POC-ים שאומתו, ולקחים מהיסטוריה קודמת (חשוב במיוחד: הסעיף "היסטוריה שקדמה לפרויקט הזה").
2. `family_hub_dashboard.html` (בתיקייה הזו) — סקיצת UI/UX מאושרת. **נקודת המוצא לעיצוב, לא לבנות מאפס.**
3. המסמך החי (ERD + כל ההחלטות): https://claude.ai/code/artifact/25a6b84c-6c11-4dce-99c8-651a5b161370

## מה זה שלב 1, ומה זה **לא**

שלב 1 הוא **אפליקציית שלד מקומית**, שרצה לגמרי בצד הלקוח (הטלפון/מחשב), בלי שום תלות בענן.

**בתחום (Scope IN):**
- ממשק PWA שאפשר להתקין למסך הבית (טלפון + מחשב + טאבלט).
- ניהול מלא (הוספה/עריכה/מחיקה/סימון) של ארבעת סוגי הפריטים: שגרות יומיות בלי שעה קבועה, משימות חד-פעמיות עם תאריך/שעה, משימות תהליכיות עם תתי-משימות, פרויקטים (כמה משימות עם מעקב התקדמות).
- שמירה מקומית ועקבית בין הפעלות — `localStorage` או `IndexedDB` (עדיף IndexedDB אם יש הרבה רשומות/תתי-משימות — יציב יותר וללא מגבלת גודל נוקשה).
- **התראות מקומיות בלבד**: תזכורות שמופעלות ע"י האפליקציה עצמה כשהיא פתוחה/ברקע דרך Service Worker (Notification API + `setTimeout`/בדיקת תאריכים בכל טעינה). **לא** Push שמגיע מהשרת כשהאפליקציה סגורה לגמרי — זה שלב אחר.
- רשימת קניות עם סטטוס (במלאי/חסר/בעגלה).

**מחוץ לתחום (Scope OUT) — במפורש, לא לגעת בזה בשלב הזה:**
- שום חיבור ל-Google (לא Sign-In, לא Calendar, לא Drive).
- שום Firebase / סנכרון בין מכשירים / מסד נתונים בענן.
- שום Push שרת-מבוסס (Service Account, FCM Admin SDK וכו').
- שום המשך על Apps Script / Make.com הקיימים — הם קפואים כרפרנס, לא חלק מהשלב הזה.

## מבנה הנתונים (סכימה מקומית)

הישות והשדות מבוססים על ה-ERD בגיליון האמיתי (`Family_OS_Database`, ראו `CLAUDE.md`), אבל **מדובר בעותק מקומי עצמאי** לשלב הזה — אין קריאה/כתיבה לגיליון האמיתי בשלב 1. מבנה מוצע (אפשר להתאים לפי מה שקיים כבר ב-`family_hub_dashboard.html`):

- **Categories**: מזהה, שם, אייקון.
- **People**: לירן / מורן (קבועים, לא רשימה פתוחה).
- **Tasks**: מזהה, שם, קטגוריה, **אחראי יחיד** (לירן / מורן — לעולם לא "שניהם" ולא ריק), סטטוס, תאריך יעד, תדירות (חד-פעמי/יומי/שבועי/חודשי), "צעד הבא" (טקסט קצר), עדיפות, הערות.
- **SubTasks**: קישור למשימת-אב, טקסט, סטטוס (בוצע/לא).
- **Routines**: שגרה חוזרת בלי שעה קבועה, קטגוריה, אחראי, תדירות.
- **RoutineCompletions**: לוג של מתי שגרה סומנה כבוצעה.
- **Projects**: שם, קטגוריה, אחראי מוביל, סטטוס, תאריך יעד, תקציב (אם רלוונטי), הערות/קישורים; מכיל כמה Tasks.
- **Shopping_List**: שם מוצר, קטגוריה, כמות, סטטוס, חנות יעד, תמונה (אופציונלי).

**כלל ברזל שחייב להיאכף בממשק (לא רק להיות שדה בטבלה): לכל Task/Project יש אחראי יחיד — הטופס לא מאפשר לשמור בלי לבחור אחראי, ואין אופציית "שניהם".** זה לקח ישיר מהיסטוריית הפרויקט — ראו `CLAUDE.md`.

אם יש קטגוריית פיננסים: תצוגה למורן (ולירן) נשארת מופשטת — 2-3 מדדים ויזואליים (תקציב פנוי, % יעד חיסכון, החלטות פתוחות) ולא טבלת נתונים גולמית.

## עקרונות עבודה — לא לחזור על טעויות עבר

1. **בלי קפיצה בין כלים/פריימוורקים באמצע.** לבחור סטאק אחד (למשל: HTML/CSS/JS וניל, או React קל, IndexedDB, Service Worker) ולהתחייב אליו לכל אורך השלב. אם מתעורר רצון לשנות גישה — לעצור ולשאול את לירן במפורש, לא לבצע בשקט.
2. **בלי שיתוק על שכלול הסכימה.** מתחילים עם הסכימה למעלה, בונים משהו עובד, ומתעדכנים תוך כדי לפי צורך אמיתי — לא מנסים לנעול את כל השדות מראש.
3. **שקיפות מלאה בין "מוקאפ" ל"עובד באמת".** בכל דיווח התקדמות ללירן: לציין במפורש אילו חלקים באמת שומרים/טוענים נתונים מול IndexedDB/localStorage, ואילו הם רק תצוגה ויזואלית עדיין לא מחוברת.
4. **התקדמות בצעדים קטנים ומאומתים** — כמו ה-POC-ים שכבר בוצעו בפרויקט (ראו `CLAUDE.md`): לבנות ישות אחת, לוודא שהיא עובדת מקצה לקצה (הוספה→שמירה→טעינה מחדש→עדיין שם), ורק אז לעבור לישות הבאה.

## קריטריון סיום לשלב 1

לירן יכול להתקין את האפליקציה על הטלפון (ושל מורן, גם אם עדיין רק אצלו לבדיקה), להוסיף/לערוך/למחוק פריט מכל אחד מ-4 הסוגים, לסגור ולפתוח מחדש את האפליקציה ולראות שהנתונים נשמרו, ולקבל תזכורת מקומית על משימה עם תאריך יעד קרוב. רק אחרי שזה עובד בנוח — עוברים לשלב 2 (Firebase).

## הערה לגבי כלים/סקילים נוספים

לירן ציין כמה סקילים (hebrew-chatbot-builder, israeli-product-price-comparator, israeli-spreadsheets, skill-creator, ועוד) שיכולים להיות רלוונטיים בהמשך הדרך — למשל ממשק צ'אט בעברית לאפליקציה, השוואת מחירים לרשימת הקניות, או ייצוא לוחות בעברית. **לא רלוונטי לשלב 1** — לשקול רק כשמגיעים לשלב שבו יש להם ערך ממשי.
