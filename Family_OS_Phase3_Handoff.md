# Family OS — מסמך מסירה לשלב 3 (Google Sign-In + יומן + דרייב)

מסמך זה מיועד לסוכן קוד (Claude Code / Cursor) שיבנה את **שלב 3** — השלב האחרון בתוכנית שסוכמה עבור Family OS. נועד להיקרא **לצד** `CLAUDE.md`, `Family_OS_Phase1_Handoff.md` ו-`Family_OS_Phase2_Handoff.md` שבאותה תיקייה — לא במקומם. שלב 2 חי, פרוס, ומאומת: https://liranmua.github.io/family-os/ (סנכרון בזמן אמת בין לירן ומורן, 14 פריטים משותפים).

## קרא קודם

1. `CLAUDE.md` — הקשר מלא, כולל סעיפי שלב 1 ושלב 2.
2. `Family_OS_Phase2_Handoff.md` — מודל האבטחה הנוכחי (נתיב קבוע בקוד, בלי login) שהשלב הזה **מחליף**.
3. המסמך החי (ERD + כל ההחלטות): https://claude.ai/code/artifact/25a6b84c-6c11-4dce-99c8-651a5b161370

## מה זה שלב 3, ומה זה **לא**

שני נושאים בלבד, בכוונה — לא לגלוש לשום דבר נוסף:

**1. Google Sign-In אמיתי — סוגר את פרצת האבטחה משלב 2 (לירן ביקש זאת במפורש).**
- מחליף את "הנתיב הפרטי הקבוע בקוד" (שכל מי שקרא את קוד ה-JS הציבורי יכול היה טכנית לגשת אליו) ב-**Firebase Authentication עם Google Sign-In**, כך שרק שני חשבונות Google ספציפיים (של לירן ושל מורן) יכולים לגשת לנתונים.
- **Security Rules מתעדכנים** מ"כל מי שיודע את הנתיב" ל-בדיקה מפורשת של `request.auth.token.email` מול רשימה סגורה של שני כתובות המייל שלכם (לא "כל מי שמחובר עם Google" — רק שניכם, בשם).
- מסך ה-login עצמו הוא אותה לחיצת "Sign in with Google" חד-פעמית שכבר מוכרת לכם מה-POC-ים הקודמים (Apps Script, Firebase POC) — לא תהליך חדש/מסובך.
- **פרט טכני חשוב**: OAuth consent screen של הפרויקט חייב להיות במצב "In production" (לא "Testing") — אחרת ה-refresh token פג כל 7 ימים וזה יאלץ login מחדש כל שבוע. זה כבר תועד ב-`CLAUDE.md` מה-POC המקורי של Push.
- **פריט פתוח שחסר**: כתובת המייל של מורן ב-Google (לירן — יש לספק לסוכן כשהוא מגיע לזה, כדי לבנות את רשימת ה-allowlist ב-Security Rules).

**2. חיבור ליומן Google — קריאה בלבד (הוחלט במפורש, לא כתיבה).**
- האפליקציה **קוראת** אירועים מיומן Google של כל אחד (Calendar API, scope `calendar.readonly` בלבד) ומציגה אותם לצד המשימות/שגרות הרלוונטיות (למשל: משימה עם תאריך יעד מוצגת יחד עם אירועים סמוכים מהיומן האמיתי).
- **האפליקציה לא יוצרת, לא עורכת ולא מוחקת שום דבר ביומן.** אם בעתיד ירצו את זה — זו החלטה נפרדת, לא חלק משלב 3.
- דרייב (כפי שתוכנן במקור): עיון/חיפוש בתיקיית Drive משותפת + כפתור ייצוא/שמירה — נשאר כפי שתוכנן, לא השתנה בסבב הזה.

**מחוץ לתחום (Scope OUT) — במפורש:**
- שום כתיבה/עריכה של אירועי יומן.
- שום Push שרת-מבוסס.
- שום שינוי בסכימת הנתונים או ב-CRUD הקיים של שלב 1/2 — שלב 3 מוסיף שכבת זהות + תצוגת יומן/דרייב, לא נוגע במבנה המשימות/שגרות/פרויקטים עצמו.

## נקודת תיאום קריטית — סדר החלפת מודל האבטחה

בניגוד למיגרציית הנתונים בשלב 2 (שם הסיכון היה איבוד נתונים), הסיכון כאן הוא **נעילה זמנית בחוץ** אם ה-Security Rules מתהדקות לפני ששניכם התחברתם ולו פעם אחת עם Google:

1. הסוכן בונה ופורס קודם את מסך/כפתור ה-Sign-In, **בלי** להדק עדיין את ה-Security Rules (הנתיב הקבוע מהשלב הקודם ממשיך לעבוד במקביל).
2. **שניכם** מתחברים לפחות פעם אחת עם Google במכשיר שלכם (לירן קודם, כרגיל, ואז מורן) ומוודאים שהאפליקציה מזהה אתכם.
3. **רק אחרי ששניכם התחברתם בהצלחה** — הסוכן מהדק את ה-Security Rules לרשימת המייל הסגורה ומבטל את גישת הנתיב-הקבוע-בלי-login. זה השלב שבו פרצת האבטחה משלב 2 נסגרת בפועל.

הסוכן צריך לעדכן אתכם באיזה שלב בדיוק נמצאים לפני שהוא נועל — בדיוק כמו העיקרון של שלב 2 ("צעדים קטנים מאומתים", לא לקפוץ לסוף בלי אימות ביניים).

## קריטריון סיום לשלב 3

לירן ומורן, כל אחד במכשיר שלו, מתחברים עם חשבון ה-Google האישי שלהם (לא עוד "נכנסים סתם"), ורואים את אותם הנתונים המשותפים כמו בשלב 2. לצד המשימות מוצגים אירועי יומן אמיתיים (קריאה בלבד) מהיומן של כל אחד. תיקיית הדרייב המשותפת ניתנת לעיון/חיפוש מתוך האפליקציה. אם מנסים לגשת מחשבון Google שלישי (לא לירן/מורן) — הגישה נדחית ע"י Security Rules. רק אחרי שזה עובד בנוח — שלב 3 (וכל שלושת השלבים המתוכננים) הושלם.

## הערה לגבי שיפורי UX/תיקונים

לירן ציין שאחרי שלב 3 הוא רוצה לעבור להתייחסות לשינויים/תיקונים בממשק ובחוויית השימוש (לא רק תשתית) — זה יגיע כרשימה נפרדת בהמשך, לא חלק ממסמך המסירה הטכני הזה.
