# Family OS — שלוש קטגוריות פריטים בפרויקט: קניות מתויגות + רשימת אריזה (שדרוג כללי, לא רק לקמפינג)

עלה מתוך שימוש אמיתי: לירן הוסיף פרויקט "קמפינג" וצירף (דרך חיבור ה-Drive שזה עתה נבנה) את קובץ "ציוד קמפינג.xlsx" האמיתי שהוא ומורן כבר משתמשים בו. **קראתי את הקובץ בפועל** (דרך חיבור Drive נפרד שיש לי בסביבת העבודה הזו, לא דרך האפליקציה) כדי לוודא שההחלטות כאן מתאימות למציאות, לא רק לתיאוריה. מצאתי בו משהו מאוד מועיל: לירן ומורן כבר מנהלים ידנית בדיוק את אותו מיון לשלוש קטגוריות שדנו עליו, עם קוד-צבעים משלהם (מצוין כהערה בקובץ עצמו): **"תכלת- מאורגן בתיקים, אדום- צריך לקנות, לבן- צריך לסדר"**. זה מאמת בדיוק את שלושת הסוגים:

1. **אדום = צריך לקנות** → פריט קניות מתויג לפרויקט (מפורט למטה).
2. **לבן = צריך לסדר/להכין** → משימה מקושרת לפרויקט (כבר נתמך, פיצ'ר קיים — אין צורך בשינוי).
3. **תכלת = כבר מאורגן בתיקים** → זה בדיוק ה"באקט השלישי" שלירן ציין: פריט שכבר יש, רק מסמנים שנלקח. פיצ'ר חדש: **רשימת אריזה** (מפורט למטה).

הקובץ כולל גם תכנון ארוחות מפורט לפי יום ושעה (למשל "הכנה ביום רביעי בערב") — זה כבר מתאים בדיוק לשדות התאריך+שעה הקיימים במשימות, אין צורך בשום דבר חדש בשביל זה.

**זה פיצ'ר כללי לכל פרויקט עתידי, לא ייחודי לקמפינג.**

## חלק א': תיוג פריטי קניות לפרויקט

פריט קניות מקבל שדה אופציונלי חדש "**שייך לפרויקט**" (בחירה מתוך רשימת הפרויקטים הקיימים, כמו שדה `ProjectID` שכבר קיים אצל משימות). כשמוגדר:

- הפריט **ממשיך להופיע ברשימת הקניות הרגילה** (לפי סוג החנות שלו, בדיוק כמו היום) — **עם תגית קטנה** שמציגה את שם הפרויקט (למשל "🏕️ קמפינג"), כדי לדעת מיד בזמן קנייה בפועל שזה קשור למשהו ספציפי.
- הפריט **מופיע גם בתוך מסך הפרויקט עצמו**, באותו אזור שבו כבר מוצגות המשימות המקושרות (checklist), כרשימה משלו — "פריטי קניות לפרויקט" — עם אפשרות לסמן "נקנה" ישירות משם, בדיוק כמו שסימון משימה כהושלמה כבר עובד גם מתוך הפרויקט וגם מתוך מסך המשימות.

זו בדיוק אותה תבנית "נתון אחד, שתי תצוגות" שכבר קיימת בין משימות לפרויקטים — לא מנגנון חדש, רק החלה שלה גם על קניות.

**מה לבנות (חלק א')**:

1. שדה אופציונלי חדש בפריט קניות: `linkedProjectId` (או דומה) — בחירה מתוך רשימת הפרויקטים הקיימים בעת הוספה/עריכה של פריט.
2. תגית ויזואלית קטנה עם שם הפרויקט על פריט מתויג, בתוך הרשימה הרגילה (לפי סוג חנות).
3. במסך פרטי הפרויקט: אזור "פריטי קניות" חדש (לצד "משימות מקושרות" הקיים), מציג את כל הפריטים המתויגים לפרויקט הזה, עם יכולת סימון "נקנה" ישירות משם.
4. פריט מתויג ממשיך להיספר בסאב-טוטל של הרשימה הרגילה שלו (סופר/סופר-פארם/בית-אחר) — אין שינוי בחישוב הקיים.

**מה לא בחלק א'**: לא נספר בנפרד בתוך "התקדמות הפרויקט" — פריט קניות מתויג הוא מידע נלווה, לא מדד השלמה. לא תיוג רב-פרויקטי (פריט אחד = פרויקט אחד לכל היותר, כמו משימות).

## חלק ב': רשימת אריזה בתוך הפרויקט (פיצ'ר חדש)

בניגוד לקניות, זה **לא** צריך תצוגה כפולה — אין "אזור אריזה" כללי באפליקציה מקביל לאזור הקניות, זו רשימה ששייכת אך ורק לפרויקט עצמו. לכן זה פשוט משמעותית: רשימת סימון (checklist) בתוך מסך הפרויקט, לצד "משימות מקושרות" ו"פריטי קניות" — שדות מינימליים בלבד: **שם פריט** + **סומן כארוז** (checkbox). בלי תאריך יעד, בלי "מוביל", בלי קטגוריית חנות — זה בדיוק מה שמבדיל אותה ממשימות: אין כאן שום פעולה מורכבת, רק "יש לנו את זה, נדאג לקחת".

**מה לבנות (חלק ב')**:

1. ישות חדשה `packingItems` (תת-רשימה בתוך פרויקט): שם פריט (טקסט חופשי) + סטטוס "ארוז" (checkbox).
2. אזור שלישי חדש במסך פרטי פרויקט: "רשימת אריזה" — הוספה/עריכה/מחיקה/סימון-כארוז, לצד "משימות מקושרות" ו"פריטי קניות".
3. אין תצוגה כפולה במקום אחר באפליקציה — נשאר בתוך הפרויקט בלבד.

**מה לא בחלק ב'**: לא נכנס לחישוב "התקדמות הפרויקט" (כמו קניות מתויגות — מידע נלווה, לא מדד השלמה). לא שיוך ל"מוביל" — כל אחד בבית יכול לסמן. לא כמויות/הערות (בניגוד לקניות) — פשוט ככל האפשר.

## סיכום: מה לא בסבב הזה (שני החלקים)

תכנון ארוחות כישות ייעודית — פריטי אוכל עם תזמון (כמו "הכנה ביום רביעי בערב") נכנסים כמשימות רגילות עם תאריך+שעה, שדות שכבר קיימים. אין צורך בשום מבנה חדש בשביל זה.
