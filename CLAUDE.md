# Family OS — הקשר לפרויקט (עבור Claude Code)

קובץ זה נועד להיטען אוטומטית ע"י Claude Code בתחילת כל סשן בתיקייה הזו, כדי שלא יהיה צורך להסביר מחדש את הרקע, ההחלטות וה-POC שכבר בוצע.

## מה זה הפרויקט

"Family OS" — מערכת לניהול משימות, שגרות ופרויקטים משפחתיים של לירן ומורן. המטרה: מקום אחד שמאחד ארבעה סוגי "דברים לעשות" שכיום מפוזרים בראש ובכלים שונים:

1. **שגרות יומיות** בלי שעה קבועה (בעיקר תחזוקת בית)
2. **משימות חד-פעמיות** עם תאריך/שעה (כמו Google Tasks כיום)
3. **משימות תהליכיות** עם תתי-משימות למעקב
4. **פרויקטים** — כמה משימות יחד עם מעקב התקדמות

Google Tasks/Reminders כבר עובד טוב לתזכורות יומיומיות פשוטות. מה שחסר הוא תמונה מאוחדת של כל הנושאים הפתוחים בחיים המשפחתיים, עם דרגות עומק שונות (מ"לתקן נזילה" ועד "לקנות בית").

## מקור האמת הנוכחי

- **Google Sheet**: `Family_OS_Database` (id `1wwSgP69bFSWor_RcAbFaB8E4AXskDliiZjvwEBeRiOU` — **הקובץ האמיתי, native Google Sheets**. ⚠️ יש עוד קובץ עם שם זהה ו-id `1xR66OydmyyeN5dc5DIbk4P4SezEJbU9t` — זה נשאר קובץ Excel/xlsx לא-מומר; ה-Sheets API דוחה אותו עם `400 FAILED_PRECONDITION — document must not be an Office file`. לא להשתמש בו.) — טאבים (שם וגם sheetId המספרי/gid, נחוץ לקריאות API ישירות): Cover=1983854010, Categories=1877393946, **Tasks=679321151**, Shopping_List=1906341286, Finance_Summary=1115151585, Knowledge_Hub=175035966, Projects=2018629634, People=1325594355, SubTasks=687045074, Updates_Log=90561342, Routines=1499652905, RoutineCompletions=1673007222
- מבנה נתונים מלא (ERD + כל הטבלאות/שדות/החלטות) מתועד במסמך חי:
  **https://claude.ai/code/artifact/25a6b84c-6c11-4dce-99c8-651a5b161370**
  (לקרוא את זה קודם — יש שם גם היסטוריית החלטות מלאה מכל הסבבים)

## מה כבר קיים בתיקייה הזו

- `family_hub_dashboard.html` — סקיצת HTML/JS סטטית (in-memory בלבד, לא מסונכרנת) שממחישה איך הדשבורד אמור להיראות ולהתנהג: טבלת משימות/שגרות/פרויקטים, טופס הוספה/עריכה/מחיקה לכל סוג, תצוגת פירוט. זו לא אפליקציה — זו סקיצה מאושרת שממנה בונים.

## החלטת ארכיטקטורה

**נבחר**: להמשיך לבנות את האפליקציה האמיתית **דרך Claude Code**, עם:
- **GitHub** — לקוד עצמו (גרסאות, היסטוריה)
- **Make.com** — כשכבת האינטגרציה בין Google Sheets ל-Google Tasks/Calendar (לא Zapier — נבדק ונמצא פחות מתאים בהיקף משפחתי: 100 "משימות"/חודש בחינם מול 1,000 אצל Make)

**עדכון בפועל**: בשלב הביניים (בדיקת סולו, לפני שהכול עובר ל-Claude Code המלא) בפועל כן נבנה Google Apps Script Web App בינארי, bound ל-`Family_OS_Database` עצמו (טאב Tasks בלבד בשלב זה) — ראו בסעיף "פרוסה אנכית ב-Apps Script" למטה. זה לא ביטול ההחלטה למעלה, אלא צעד ביניים ממוקד לפני שממשיכים; הקוד יעבור ל-clasp+git תחת Claude Code בהמשך (ראו `apps-script/README.md`).

## ה-POC שבוצע ואומת (חשוב!)

ההנחה הכי מסוכנת בכל התכנון — "אפשר לחבר שורה בגיליון Google Sheets למשימה אמיתית ב-Google Tasks, בצורה יציבה, דרך Make" — **נבדקה בפועל ועברה**, פעמיים, כולל מיפוי מלא (שם + סטטוס + תאריך יעד).

פרטים טכניים מדויקים לשחזור/המשך:

| מה | ערך |
|---|---|
| Make Organization ID | `5753962` |
| Make Team ID | `496136` |
| חשבון Google מחובר | liranmua@gmail.com |
| Scenario לדוגמה | id `7179788`, "POC - גיליון משפחה ל-Google Tasks" |
| חיבור Google Sheets ב-Make | connection id `10386080` |
| חיבור Google Tasks ב-Make | connection id `10386093` |
| גיליון הבדיקה (לא האמיתי!) | `Family_OS_POC_Test`, id `1YepNLzT8M9dNs1enu6xArnjGfvnSHorexiYqSubzevI`, טאב `Untitled`, עמודות A=משימה B=סטטוס C=תאריך יעד |
| מודולים שאומתו | `google-sheets:watchRows` (v2), `google-tasks:createATask` (v5) |

**גוצ'ה קריטית לזכור**: התייחסות לעמודה לפי אינדקס מספרי חייבת אפוסטרופים הפוכים ב-Make: `{{1.`0`}}` ולא `{{1.0}}` — הצורה השנייה מתפרשת כמספר עשרוני (1.0) ושוברת את המיפוי. תאריך הומר ל-RFC3339 בהדבקת מחרוזת: `{{1.`2`}}T00:00:00.000Z`.

## פרוסה אנכית ב-Apps Script (בוצעה, על הגיליון האמיתי)

במקביל ל-POC של Make, הורצה גם פרוסה אנכית ישירה: Apps Script Web App **bound** ל-`Family_OS_Database` האמיתי (לא גיליון בדיקה), טאב `Tasks` בלבד, גישה "Only myself" (סולו). קוד מלא ב-`apps-script/Code.gs` + `Index.html` + `README.md` (תיקייה זו). פונקציות עיקריות: `getTasks()`, `addTask()` (משתמש ב-`appendRow` — מדלג על Data Validation), `updateTask()` (כותב תא-תא עם `setValue()`, אוסף כשלי Validation ל-`warnings[]` בלי להפיל את כל העדכון), `getLists()` (מחזיר ערכים חוקיים חיים מהטאבים Categories/People/Projects, לא רשימה מקודדת).

**באג שנמצא ותוקן**: ל-Data Validation בעמודות C (Category), M (ProjectID), N (RelatedPerson) בטאב Tasks היו כללים שהתייחסו לתחביר טבלת Excel שבור (`tblCategories[CategoryName]` וכו') ששרד מהמרת ה-xlsx המקורי. `appendRow` מדלג על בדיקת Validation (ולכן `addTask` עבד), אבל `setValues`/`setValue` אוכף אותו (ולכן `updateTask` נכשל בשקט/חלקית על השדות האלה). **תוקן ב-2026-09-04**: ה-Data Validation בשלוש העמודות (C/M/N, שורות 2–1000) **נמחק** ישירות דרך קריאת `spreadsheets.batchUpdate` ל-Sheets API (בוצעה דרך Make Tool, connection `10386080`, לא ידנית) — כי הטופס באפליקציה עצמה כבר מגביל את הקלט לערכים חוקיים, כך שה-Validation ברמת הגיליון היה יתיר. **חשוב לבדוק**: אם רוצים להחזיר Validation תקין (למשל כדי להגן גם על עריכה ידנית בגיליון עצמו, לא רק דרך האפליקציה) — זה עדיין פתוח, ולא סביר שנעשה אוטומטית; זה צעד מכוון להשאיר את הטופס כקו ההגנה היחיד בינתיים.

## מה עדיין לא אומת / שלבים הבאים

1. **תזמון אוטומטי (Make)** — התרחיש עדיין "on-demand" בכוונה. לירן ביקש לא לעבור לתזמון קבוע (המינימום בחינם: כל 15 דקות) לפני שרואים שהכול עובד כמו שצריך.
2. **חיבור ה-POC של Make לגיליון האמיתי** — ה-POC של Make רץ על גיליון בדיקה נפרד (`Family_OS_POC_Test`), לא על `Family_OS_Database`. (הפרוסה ב-Apps Script כן רצה על הגיליון האמיתי — ראו למעלה.)
3. **חיבור מורן** — כרגע רק חשבון הגוגל של לירן מחובר (גם ב-Make וגם ב-Apps Script deployment). צריך חיבור/שיתוף נפרד למורן — וגם לשנות `Who has access` בפריסת ה-Web App מ-"Only myself" בהתאם.
4. **כיוון הפוך (Make)** — עדכון/מחיקה בגיליון עדיין לא גורם לעדכון/מחיקה במשימה ב-Google Tasks דרך Make (רק יצירה נבדקה שם).
5. **סנכרון דו-כיווני מלא לדשבורד ה-HTML** — `family_hub_dashboard.html` עדיין in-memory; לא כתוב חזרה ל-Sheet. הפרוסה ב-Apps Script (Index.html) היא התחלה נפרדת וממוקדת יותר (טאב Tasks בלבד), לא החלפה של הדשבורד המלא.
6. **מעבר ל-clasp + git** — מתוכנן אחרי כשבוע שימוש יציב בפרוסה הסולו (ראו `apps-script/README.md`).

## עדכון כיוון (2026-09-05): מעבר ל-PWA עצמאי, בנוי לפי שלבי קושי

לירן שאף לגובה: אפליקציה משפחתית עצמאית (לא רק טאב Tasks), שיושבת על הטלפון שלו ושל אשתו + מחשב + טאבלט, עם מסד נתונים פנימי, ייצוא בלחיצה לדרייב, והתראות Push — וגם חיבור לחשבונות הגוגל של שניהם (יומן, דרייב, חיפוש מסמכים). בוצע מחקר וגם **POC אמיתי** של פוש (ראו הערה למטה) — ואז הוחלט במפורש **לדחות את חלק ה-Push העצמי (שרת) לשלב מאוחר יותר**, כי הוא היחיד שדורש ניהול מפתח שרת (Service Account) — כל שאר הדרישות (Sign-In אישי, יומן, דרייב, מסד נתונים) לא דורשות שום קובץ מפתח, רק אותה לחיצת אישור OAuth חד-פעמית שלירן כבר מכיר.

**עדכון (2026-09-07): שלב 1 חי ופרוס בפועל.** ה-Prototype PWA עלה בהצלחה ל-GitHub Pages, דרך repo נפרד (`liranmua/family-os`, ציבורי, עם GitHub Actions workflow שכבר קיים ב-repo ל-build+deploy אוטומטי בכל push ל-main): **https://liranmua.github.io/family-os/** — לירן אישר שהאתר עולה בטלפון שלו, נראה טוב, ומרגיש מחובר. זה עדיין באותו scope של שלב 1 (בלי ענן/גוגל) — רק שקיבל כתובת HTTPS אמיתית במקום localhost, כדי שאפשר יהיה להתקין אותו כ-PWA למסך הבית. המשך עבודה על השלד (טופס לוג עדכונים, תצוגת פיננסים מופשטת) ממשיך מכאן — כל push חדש ל-main מתפרסם אוטומטית לאותה כתובת תוך דקה-שתיים.

**תוכנית מוסכמת, שלושה שלבים לפי קושי (לא לפי סדר החזון המקורי):**

1. **שלב 1 — Prototype PWA מקומי**: סוכן קוד (Claude Code / Cursor) בונה את הממשק הראשי — משימות, רשימת קניות, פרויקטים, **התראות מקומיות בלבד** (in-app, לא פוש משרת). **בלי ענן, בלי חשבונות גוגל בשלב הזה בכלל.** נקודת ייחוס לעיצוב/UX: `family_hub_dashboard.html` הקיים — לא לבנות מאפס, להתפתח ממנו.
2. **שלב 2 — Firebase**: סנכרון בזמן אמת בין המכשיר שלו ושל אשתו, דרך Firestore (יש כבר פרויקט Firebase חינמי מוכן מה-POC, לא צריך להקים מחדש — ראו טבלה למטה).
3. **שלב 3 — Google APIs**: חיבור יומן ודרייב, per-user Sign-In (לא backend חד-פעמי) — קריאת יומן, שמירה/חיפוש בתקיית דרייב משותפת.

**Push (התראות שהשרת שולח בלי שהדף פתוח) — במפורש מחוץ לתחום כרגע**, ואם/כשירצה לחזור אליו: יש כבר תשתית מוכנה (ראו טבלה).

**מה כבר קיים ומוכן לשימוש חוזר (מה-POC של Push, לא לזרוק):**

| מה | ערך |
|---|---|
| Firebase project | `family-os-poc` (Spark — חינמי, בלי כרטיס אשראי) |
| Hosting URL (דף בדיקה, לא האפליקציה) | https://family-os-poc.web.app |
| firebaseConfig (לא סוד — מזהה ציבורי, מותר לחשוף בקוד קליינט) | apiKey/authDomain/projectId/... — בקובץ `poc-push/public/firebase-config.js` בתיקיית הפרויקט |
| Service Account key שהורד ל-Push | **רגיש** — נועד רק לבדיקת ה-POC; מומלץ למחוק מ-Firebase Console → Project settings → Service accounts כשנוח, לא דחוף |
| גוצ'ה שנתקלנו בו | סביבת הענן שלי (Cowork) חסומה מלהתחבר ל-oauth2.googleapis.com — כל דבר שדורש Service Account/Admin SDK צריך לרוץ במחשב של לירן, לא כאן |

**שולי ביחס למה שכבר קיים**: הפרוסה ב-Apps Script (טאב Tasks על הגיליון האמיתי) ותרחיש ה-Make (Sheets→Tasks) עדיין קיימים ותקינים כרפרנס/גיבוי, אבל **ההמשך שלהם (clasp+git, פורטים נוספים כמו Routines) מוקפא בינתיים** לטובת הכיוון החדש הזה. אין צורך למחוק אותם.

## עקרונות עבודה שחשוב לשמר

- **לירן מזהה בעצמו דפוס של קפיצה למשהו גדול בלי לבדוק חלקים בודדים קודם** — ומבקש שיעצרו אותו מזה. לכל שלב חדש: לוודא חלק קטן וממוקד לפני בנייה רחבה.
- הטבלה (Sheet) היא "הלב" — כל דבר אחר (דשבורד, Make, אפליקציה) צריך להישאר מסונכרן אליה, לא להחליף אותה כמקור אמת.
- ה-HTML dashboard הקיים הוא נקודת ייחוס מאושרת לעיצוב/UX — לא לבנות מחדש מאפס, אלא להתפתח ממנו.

## היסטוריה שקדמה לפרויקט הזה (חשוב לא לחזור על אותן טעויות)

לירן שיתף קובץ PDF (61 עמודים) עם היסטוריית שיחה מלאה מג'ימיני, שמתעדת ניסיונות קודמים לבנות בדיוק את הפרויקט הזה — לפני שהגיע ל-Claude בכלל. נקרא במלואו (2026-09-05). הקובץ נמצא בתיקיית הפרויקט: `בניית אפליקציית ניהול משק בית משפחתי - Google Gemini.pdf`. תמצית הלקחים:

- **היו לפחות שני ניסיונות קודמים שננטשו.** ניסיון ראשון: AppSheet ישירות על Google Sheets — נתקל בשגיאות טכניות חוזרות (Ref/Key בין טבלאות, שינויי שמות עמודות של Google Calendar בין סוגי חשבונות, שגיאת "Color type", כפתור View מחובר לטבלה הלא נכונה, הרשאות יומן חלקיות). אף אחת מהן לא הייתה קטלנית לבד, אבל ההצטברות שחקה אותו.
- ניסיון שני, מסודר יותר: ג'ימיני שימש כ"מנהל מוצר וארכיטקט", ובנה PRD מלא ("Family OS") אחרי ניתוח למה מערכות ניהול בית משפחתיות ננטשות (חוסר בעלות יחידה על משימה, הימנעות של בן/בת הזוג מטבלאות פיננסיות מורכבות, מטרות גדולות בלי "צעד בא" קטן, חוסר שגרת פתיחה/סגירת יום, חלוקת מטלות לא ברורה, שיחת סנכרון שבועית שהופכת ל"אירוע מעייף" במקום הצצה של 5 דקות). **"עקרונות הברזל" מה-PRD ששווה לשמר בעיצוב הנוכחי:**
  - לכל משימה אחראי יחיד — אף פעם לא "שנינו"/לא משויך.
  - מטרות/פרויקטים גדולים תמיד מתפרקים ל"צעד הבא" אחד שלוקח פחות מ-15 דקות.
  - תצוגת פיננסים לבן/בת הזוג נשארת מופשטת (2-3 מדדים ויזואליים: תקציב פנוי, % יעד חיסכון, החלטות שדורשות שיתוף) ולא טבלאות גולמיות.
  - חלוקת תחומים ברירת מחדל: לירן — פיננסים, תחזוקת בית/רכב, ביטוחים, בריאות; אשתו — קניות, ציוד ילדים, ניקיון/תחזוקה שוטפת; משותף — ילדים/מסגרות, פרויקטים, יומן.
  - שגרת סנכרון שבועית של 15 דקות, מוזנת מתדריך קצר שסוכן מכין מראש (משימות פתוחות לכל אחד + צעד בא אחד לנושא שנזנח) — לא פתיחת טבלאות גולמיות.
- **הדפוס שזוהה כגורם הישיר לנטישות בעבר: קפיצה בין כלים/פלטפורמות תוך כדי עבודה** (Excel↔Google Sheets↔AppSheet↔Google AI Studio, הלוך ושוב), עד כדי בלבול גם אצל ג'ימיני עצמו. לירן אמר במפורש: "קפצתי בין האפשרויות... נתקעתי בעבר וזה ננטש". **לקח: להתחייב למסלול אחד לכל שלב, לא לפתוח מחדש החלטות ארכיטקטורה שכבר סוכמו באמצע העבודה.**
- דפוס נוסף: שיתוק מניסיון לסגור את כל מבנה הנתונים/העמודות מראש לפני שממשיכים ("לא רוצה שהחשיבה על הטבלאות תתקע את הפרויקט") — נפתר ע"י פיצול למסלולים מקבילים נמוכי-חיכוך במקום לנסות לשכלל את הסכימה מראש.
- רגע של "התקדמות מדומה": Google AI Studio ייצר סקיצת אתר מרשימה שלירן חשב בהתחלה שכבר מחוברת לנתונים האמיתיים — היא לא הייתה, רק מוקאפ ויזואלי בלי חיבור אמיתי לגיליון/יומן/דרייב. **לקח: תמיד להיות מפורש מה באמת חי/מחובר מול מה שהוא רק מוקאפ.**
- **ההודעה האחרונה בהיסטוריה הזו (ממש לפני שלירן פתח את Claude Code בכלל) היא אותה תוכנית שלושת השלבים** שכבר אימצנו בפרויקט הזה (Prototype PWA מקומי → Firebase → Google APIs). זה לא רעיון חיצוני חדש — זה ממש המסקנה שאליה הגיעה שיחת התכנון בג'ימיני, מה שמסביר למה זה התאים כל כך טוב למה שכבר התכנסנו אליו באופן עצמאי.
