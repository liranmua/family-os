# OS / editor
.DS_Store
Thumbs.db
*.swp

# Node (only used for a local static server, no build step)
node_modules/
npm-debug.log*

# Local scratch
scratch/
*.local

# Firebase CLI cache
.firebase/
firebase-debug.log
firestore-debug.log
